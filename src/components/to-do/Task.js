const Task = (props) => {
    console.log('this is props in ToDoList', props)
    return(
        <>
            <div>
                this is a task
            </div>
        </>
    )
}

export default Task